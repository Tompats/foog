---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const { title, description } = Astro.props;
const defaultName = 'Φ.Ο.Ο.Γ - Φυσιολατρικός Ορειβατικός Όμιλος Γιαννιτσών';
const pageTitle = title ? `${title} | ${defaultName}` : defaultName;
const metaDescription =
  description ??
  'Εθελοντικός ορειβατικός όμιλος Γιαννιτσών με εξορμήσεις, εκπαίδευση ασφάλειας σε χιόνι και ανοιχτή κοινότητα φυσιολατρών.';

const navLinks = [
  { href: '/', label: 'Αρχική' },
  { href: '/trips', label: 'Εξορμήσεις' },
  { href: '/history', label: 'Ιστορία' },
  { href: '/coaches', label: 'Προπονητές' },
  { href: '/board', label: 'Διοίκηση' },
  { href: '/join', label: 'Γίνε μέλος' },
  { href: '/gallery', label: 'Γκαλερί' },
  { href: '/contact', label: 'Επικοινωνία' }
];
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{pageTitle}</title>
    <meta name="description" content={metaDescription} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cabin:wght@400;500;600;700&family=Space+Grotesk:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="relative min-h-screen text-brand-rocky">
    <div class="absolute inset-0 -z-10">
      <div class="h-96 w-full bg-gradient-to-b from-brand-moss/30 via-transparent to-transparent blur-3xl"></div>
      <div class="absolute inset-x-0 bottom-0 h-64 bg-gradient-to-t from-brand-river/20 to-transparent pointer-events-none"></div>
    </div>
    <div class="relative flex min-h-screen flex-col">
      <header class="section-padding pb-6">
        <div class="glass-panel px-6 py-6 lg:py-5">
          <div class="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between">
            <div>
              <a href="/" class="text-2xl font-semibold text-brand-pine heading-font flex items-center gap-3">
                Φ.Ο.Ο.Γ
                <span class="playful-pill">since 1974</span>
              </a>
              <p class="text-sm text-brand-mist mt-1">Ορέξα για κορυφές, παρέα για πάντα.</p>
            </div>
            <div class="flex items-center gap-3">
              <button
                id="nav-toggle"
                class="lg:hidden inline-flex items-center gap-2 rounded-full border border-brand-pine/30 px-4 py-2 text-sm font-semibold text-brand-rocky hover:text-brand-terracotta"
              >
                Μενού
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="h-5 w-5">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5M3.75 17.25h16.5" />
                </svg>
              </button>
              <nav id="nav-menu" class="hidden flex-wrap items-center gap-3 text-sm font-semibold text-brand-rocky lg:flex">
                {navLinks.map((link) => (
                  <a
                    href={link.href}
                    class="rounded-full bg-brand-cloud px-4 py-2 border border-brand-river/10 shadow-sm shadow-brand-river/10 transition hover:-translate-y-0.5 hover:text-brand-terracotta"
                  >
                    {link.label}
                  </a>
                ))}
                <a
                  href="/trips"
                  class="hidden lg:inline-flex items-center gap-2 rounded-full bg-brand-terracotta px-5 py-2 text-sm font-semibold text-brand-cloud shadow-brand-terracotta/30 shadow-lg hover:translate-y-0.5"
                >
                  Νέα εξόρμηση
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="1.5">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M5 12h14m-6-6 6 6-6 6" />
                  </svg>
                </a>
              </nav>
            </div>
          </div>
          <nav
            id="nav-menu-mobile"
            class="mt-4 hidden flex-col gap-2 text-sm font-semibold text-brand-rocky lg:hidden"
          >
            {navLinks.map((link) => (
              <a
                href={link.href}
                class="rounded-2xl border border-brand-river/40 bg-brand-cloud px-4 py-3 transition hover:border-brand-terracotta/60 hover:text-brand-terracotta"
              >
                {link.label}
              </a>
            ))}
            <a
              href="/trips"
              class="rounded-2xl bg-brand-terracotta/90 text-brand-cloud px-4 py-3 text-center font-semibold shadow-md"
            >
              Νέα εξόρμηση
            </a>
          </nav>
        </div>
      </header>
      <main class="flex-1 pb-20">
        <slot />
      </main>
      <footer class="section-padding">
        <div class="glass-panel px-6 py-8 grid gap-8 lg:grid-cols-3">
          <div>
            <h3 class="text-xl font-semibold text-brand-rocky">Πάντα έτοιμοι για περιπέτεια.</h3>
            <p class="text-brand-mist mt-2">Εβδομαδιαίες συναντήσεις, ανανέωση γνώσεων χιονοστιβάδων και φροντίδα των αγαπημένων μας βουνών.</p>
          </div>
          <div class="space-y-2 text-brand-mist">
            <a href="/trips" class="block hover:text-brand-pine">Εξορμήσεις</a>
            <a href="/join" class="block hover:text-brand-pine">Γίνε μέλος</a>
            <a href="/contact" class="block hover:text-brand-pine">Επικοινωνία</a>
            <a href="/history" class="block hover:text-brand-pine">Ασφάλεια &amp; Εκπαίδευση</a>
          </div>
          <div class="text-brand-mist space-y-2">
            <p>Ηλ. ταχυδρομείο: <a href="mailto:info@mountains.club" class="font-semibold text-brand-pine">info@mountains.club</a></p>
            <p>Κοινωνικά: <a href="#" class="hover:text-brand-pine">Instagram</a> · <a href="#" class="hover:text-brand-pine">Facebook</a></p>
            <p class="text-sm text-brand-mist">Τα ορεινά σπορ ενέχουν κινδύνους· συμμετέχετε εντός των ορίων σας και σύμφωνα με τα πρωτόκολλα του Φ.Ο.Ο.Γ.</p>
          </div>
        </div>
      </footer>
    </div>
    <script>
      const toggle = document.getElementById('nav-toggle');
      const mobile = document.getElementById('nav-menu-mobile');
      toggle?.addEventListener('click', () => {
        mobile?.classList.toggle('hidden');
      });
    </script>
  </body>
</html>
