---
import type { Coach } from '../types';

interface Props {
  coach: Coach;
}

const { coach } = Astro.props;
---
<div class="glass-panel p-6 flex flex-col gap-4 text-center transition duration-300 hover:-translate-y-1 hover:border-brand-forest/50">
  <img src={coach.photoUrl} alt={coach.name} class="mx-auto h-28 w-28 rounded-full object-cover ring-4 ring-brand-sand/60" loading="lazy" />
  <div>
    <p class="text-sm uppercase tracking-[0.3em] text-brand-forest">{coach.role}</p>
    <h3 class="text-2xl font-semibold text-brand-rock">{coach.name}</h3>
  </div>
  <p class="text-brand-fog text-sm leading-relaxed">{coach.bio}</p>
  <div class="flex flex-wrap justify-center gap-2">
    {coach.certifications.map((cert) => (
      <span class="text-xs px-3 py-1 rounded-full border border-brand-sand/70 text-brand-rock bg-white/70">
        {cert}
      </span>
    ))}
  </div>
  <div class="flex flex-wrap justify-center gap-2">
    {coach.specialties.map((spec) => (
      <span class="text-xs uppercase tracking-[0.2em] text-brand-forest border border-brand-forest/40 rounded-full px-3 py-1 bg-brand-forest/5">
        {spec}
      </span>
    ))}
  </div>
</div>
